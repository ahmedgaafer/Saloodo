const init = require("./init.db");
const auth = require("./auth.db");
const parcels = require("./parcels.db");

module.exports.init = init;
module.exports.auth = auth;
module.exports.parcels = parcels;
